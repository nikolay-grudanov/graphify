openapi: 3.0.1
info:
  title: mock
  version: 1.0.0
servers:
  - url: "https://mock"
paths:
  /mock:
    head:
      summary: mock
      operationId: mock
      responses:
        "200":
          description: mock
components:
  schemas:
    KafkaHeaders:
      description: Заголовки сообщений в Kafka
      type: object
      properties:
        ibp_id:
          type: string
          format: uuid
        ibp_source:
          type: string
        ibp_type_info:
          type: string
